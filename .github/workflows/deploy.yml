name: Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        uses: wei/curl@master
        with:
          args: ${{ secrets.DEPLOY_URL }}
